[workspace]
members = [
    "game",
    "json_asset_loader",
    "bevy_ecs_ldtk",
    "bevy_sepax2d",
]
resolver = "2"

[profile.release]
opt-level = 3

# Enable high optimizations for dependencies (incl. Bevy), but not for our code:
#[profile.dev.package."*"]
#opt-level = 2